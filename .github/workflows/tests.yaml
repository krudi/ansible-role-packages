---
name: Test (Molecule)

on:
    pull_request:
        types:
            - opened
            - synchronize
            - reopened
    push:
        branches:
            - main

    schedule:
        - cron: "0 22 * * 5"

defaults:
    run:
        working-directory: "ansible-role-packages"

concurrency:
    group: test-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

permissions:
    contents: read

jobs:
    test:
        runs-on: ubuntu-latest
        timeout-minutes: 30
        strategy:
            fail-fast: false
            matrix:
                include:
                    - distro: ubuntu2404
                      playbook: converge.yml
                    - distro: ubuntu2204
                      playbook: converge.yml
                    - distro: debian12
                      playbook: converge.yml
                    - distro: debian13
                      playbook: converge.yml

        steps:

            - name: Checkout the current branch
              uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
              with:
                  path: "ansible-role-packages"

            - name: Setup of Python 3
              uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
              with:
                  python-version: "3.11"

            - name: Install Ansible dependencies
              run: python -m pip install --disable-pip-version-check --requirement requirements-ci.txt

            - name: Run Molecule test commands
              run: molecule test
              env:
                  MOLECULE_DISTRO: ${{ matrix.distro }}
                  MOLECULE_PLAYBOOK: ${{ matrix.playbook }}
                  PY_COLORS: "1"
                  ANSIBLE_FORCE_COLOR: "1"
